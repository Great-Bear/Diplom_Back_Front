import { CollectionLocation } from './collection-location';

describe('CollectionLocation', () => {
  it('should create an instance', () => {
    expect(new CollectionLocation()).toBeTruthy();
  });
});
