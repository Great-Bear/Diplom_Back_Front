import { FilterValueContainer } from './filter-value-container';

describe('FilterValueContainer', () => {
  it('should create an instance', () => {
    expect(new FilterValueContainer()).toBeTruthy();
  });
});
